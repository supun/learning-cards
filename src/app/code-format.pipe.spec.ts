import { CodeFormatPipe } from './code-format.pipe';

describe('CodeFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CodeFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
